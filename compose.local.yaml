# Infra minimale pour le dev Next.js : cache Redis (sessions chat).
# Usage : docker compose -f compose.local.yaml up -d
services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_dev:/data
    command: redis-server --appendonly yes

volumes:
  redis_dev:
